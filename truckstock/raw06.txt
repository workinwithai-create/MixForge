M(overdue().reduce((a,b)=>a+b.amt,0))}</b></div><div class="metric"><i>◷</i><span>Due in 7 days</span><b>${M(soon)}</b></div><div class="metric"><i>💳</i><span>Open balance</span><b>${M(openBills())}</b></div><div class="metric"><i>↻</i><span>Monthly recurring</span><b>${M(s.bills.filter(b=>b.repeat=='monthly').reduce((a,b)=>a+b.amt,0))}</b></div></div><div class="table" style="margin-top:12px"><table><thead><tr><th>Vendor</th><th>Category</th><th>Due</th><th>Recurring</th><th>Status</th><th class="right">Amount</th><th></th></tr></thead><tbody>${s.bills.sort((a,b)=>a.due.localeCompare(b.due)).map(b=>{let st=b.status=='paid'?'green':b.due<D()?'red':'amber';return `<tr><td><b>${b.vendor}</b></td><td>${b.cat}</td><td>${b.due}</td><td>${b.repeat}</td><td><span class="tag ${st}">${b.status=='paid'?'paid':b.due<D()?'overdue':'open'}</span></td><td class="right"><b>${M(b.amt)}</b></td><td><button class="btn s" onclick="payBill('${b.id}')">${b.status=='paid'?'Reopen':'Mark paid'}</button></td></tr>`}).join('')}</tbody></table></div>`}function payBill(id){let b=s.bills.find(x=>x.id==id);if(b.status=='paid')b.status='open';else{b.status='paid';if(b.repeat=='monthly')s.bills.push({...b,id:U('b'),status:'open',due:A(b.due,30)})}save();bills()}
function reports(){let a=[680,920,540,1180,760,1320,980],mx=Math.max(...a);v.innerHTML=`<div class="head"><div><p class="ey">Owner and office view</p><h1>Reports</h1><span class="muted">A company-level snapshot for purchasing and management.</span></div><button class="btn" onclick="backup()">Export data</button></div><div class="grid metrics"><div class="metric"><i>$</i><span>Total inventory</span><b>${M(val('Truck')+val('Shop'))}</b></div><div class="metric"><i>↗</i><span>Invoice revenue</span><b>${M(s.inv.reduce((x,i)=>x+i.total,0))}</b></div><div class="metric"><i>⚠</i><span>Low-stock exposure</span><b>${M(low().reduce((x,p)=>x+Math.max(p.reorder,p.min-stock(p,'Truck').q)*p.cost,0))}</b></div><div class="metric"><i>💳</i><span>Bills outstanding</span><b>${M(openBills())}</b></div></div><div class="grid cols"><section class="panel"><div class="head"><div><p class="ey">Demo report</p><h2>Weekly invoice volume</h2></div></div><div class="bars">${a.map((x,i)=>`<div class="bar" style="height:${x/mx*100}%"><small>${'MTWTFSS'[i]}</small></div>`).join('')}</div></section><section class="panel"><div class="head"><div><p class="ey">Operational risk</p><h2>Attention list</h2></div></div><div class="row"><span class="tag red">High</span><div class="grow"><b>${overdue().length} overdue bills</b><small>${M(overdue().reduce((a,b)=>a+b.amt,0))} requires action</small></div></div><div class="row"><span class="tag amber">Medium</span><div class="grow"><b>${low().length} low-stock truck parts</b><small>Potential service readiness risk</small></div></div><div class="row"><span class="tag amber">Medium</span><div class="grow"><b>${s.parts.filter(p=>stock(p,'Truck').z=='Unsorted / Floor').length} unsorted items</b><small>Resolve during Van Reset mode</small></div></div></section></div>`}
function intake(){let name=prompt('Part name');if(!name)return;let qty=+prompt('Quantity','1')||0,l=prompt('Location: Truck or Shop','Truck')||'Truck',z=prompt('Compartment / shelf',l=='Truck'?'Unsorted / Floor':'Shop Shelf A')||'Unsorted / Floor',p=s.parts.find(x=>x.name.toLowerCase()==name.toLowerCase());if(!p){p={id:U('p'),name,sku:prompt('SKU / part number','CUSTOM-'+(s.parts.length+1))||'',cat:prompt('Category','Uncategorized'