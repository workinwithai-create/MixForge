)||'Uncategorized',cost:+prompt('Cost each','0')||0,price:+prompt('Selling price','0')||0,min:+prompt('Minimum stock','2')||0,reorder:+prompt('Reorder quantity','4')||0,s:{Truck:{z:'Unassigned',q:0},Shop:{z:'Unassigned',q:0}}};s.parts.push(p)}p.s[l]=p.s[l]||{z,q:0};p.s[l].z=z;p.s[l].q+=qty;save();msg('Inventory saved',`${qty} × ${name} → ${z}`);render();if(confirm('Add another item?'))intake()}
function move(id){let p=id?s.parts.find(x=>x.id==id):s.parts.find(x=>x.name.toLowerCase()==(prompt('Part name to move')||'').toLowerCase());if(!p)return;let from=prompt('Move from: Truck or Shop','Truck')||'Truck',to=from=='Truck'?'Shop':'Truck',qty=+prompt(`Quantity (${stock(p,from).q} available)`,'1')||0;if(qty>stock(p,from).q)return msg('Not enough stock');let z=prompt('Destination compartment / shelf',to=='Truck'?'Unsorted / Floor':'Shop Shelf A')||'Unassigned';p.s[from].q-=qty;p.s[to]=p.s[to]||{z,q:0};p.s[to].z=z;p.s[to].q+=qty;save();msg('Transfer recorded',`${qty} × ${p.name}: ${from} → ${to}`);render()}
function countZone(){let z=zone=='All'?prompt('Zone to count','Unsorted / Floor'):zone;if(!z)return;s.parts.filter(p=>stock(p,'Truck').z==z).forEach(p=>{let n=prompt(`${p.name}: physical quantity`,stock(p,'Truck').q);if(n!==null)p.s.Truck.q=Math.max(0,+n||0)});save();msg('Physical count saved',z);truck()}
function newBill(){let vendor=prompt('Vendor / bill name');if(!vendor)return;s.bills.push({id:U('b'),vendor,cat:prompt('Category','Other')||'Other',amt:+prompt('Amount','0')||0,due:prompt('Due date YYYY-MM-DD',D())||D(),status:'open',repeat:prompt('Recurring: none, weekly, monthly, annual','monthly')||'none'});save();msg('Bill added');bills()}
function newInv(){let cust=prompt('Customer name or address');if(!cust)return;let work=prompt('Work performed','Service work')||'Service work',labor=+prompt('Labor amount','175')||0,total=labor,used=[];while(confirm('Add a truck part to this invoice?')){let name=prompt('Part name'),p=s.parts.find(x=>x.name.toLowerCase()==(name||'').toLowerCase());if(!p){msg('Part not found');continue}let n=+prompt(`Quantity (${stock(p,'Truck').q} available)`,'1')||0;if(n>stock(p,'Truck').q){msg('Not enough stock');continue}used.push([p,n]);total+=n*p.price}if(!confirm(`Finalize invoice for ${M(total)}?`))return;used.forEach(x=>x[0].s.Truck.q-=x[1]);s.inv.unshift({id:U('i'),num:'MSD-'+D().slice(5,7)+D().slice(8)+'-'+String(s.inv.length+1).padStart(2,'0'),cust,work,date:D(),total,status:'unpaid'});save();msg('Invoice finalized',`${M(total)} billed and stock updated`);invoices()}
function backup(){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(s,null,2)],{type:'application/json'}));a.download='truckstock-backup-'+D()+'.json';a.click()}render();
</script></body></html>