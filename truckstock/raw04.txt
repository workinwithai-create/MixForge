ll>Item, quantity, and compartment</small></span>›</button><button onclick="go('truck')"><span class="ic">🚐</span><span><b>Visual truck map</b><small>Tap a compartment to inspect it</small></span>›</button><button onclick="move()"><span class="ic">⇄</span><span><b>Truck ↔ shop transfer</b><small>Move stock and preserve totals</small></span>›</button><button onclick="newInv()"><span class="ic">▤</span><span><b>New invoice</b><small>Deduct stock when finalized</small></span>›</button></div></section><section class="panel"><div class="head"><div><p class="ey">Needs attention</p><h2>Owner view</h2></div></div>${overdue().map(b=>`<div class="row"><span class="tag red">Overdue</span><div class="grow"><b>${b.vendor}</b><small>${M(b.amt)} due ${b.due}</small></div></div>`).join('')}${low().slice(0,4).map(p=>`<div class="row"><span class="tag amber">Low</span><div class="grow"><b>${p.name}</b><small>${stock(p,'Truck').q} in ${stock(p,'Truck').z}</small></div></div>`).join('')}</section></div>`}
function zs(n){let p=s.parts.filter(x=>stock(x,'Truck').z==n&&stock(x,'Truck').q>0);return{p,u:p.reduce((a,x)=>a+stock(x,'Truck').q,0),l:p.filter(x=>stock(x,'Truck').q<=x.min).length}}function zb(n,c){let x=zs(n);return `<button class="zone ${c} ${zone==n?'on':''} ${x.l?'low':''}" onclick="zone='${n}';truck()"><b>${n}</b><small>${x.p.length} parts • ${x.u} units</small>${x.l?`<em>${x.l} low</em>`:''}</button>`}
function truck(){let ps=(zone=='All'?s.parts:zs(zone).p).filter(p=>stock(p,'Truck').q>0);v.innerHTML=`<div class="head"><div><p class="ey">Interactive compartment map</p><h1>Truck 1</h1><span class="muted">Tap a zone, count it, or add items as they go back.</span></div><button class="btn p" onclick="intake()">＋ Add while loading</button></div><div class="truckgrid"><section class="vanbox"><div class="van"><div class="cab"><div class="seat"></div><div class="seat"></div></div><div class="cargo">${zb('Left Drawer 1','l1')}${zb('Right Drawer 1','r1')}${zb('Left Drawer 2','l2')}${zb('Right Drawer 2','r2')}${zb('Left Drawer 3','l3')}${zb('Right Drawer 3','r3')}${zb('Center Cabinet','center')}${zb('Pipe Rack','pipe')}${zb('Rear Shelf','rear')}</div></div><div class="toolbar" style="margin-top:10px"><button class="btn s" onclick="zone='All';truck()">Show all</button><button class="btn s" onclick="countZone()">Count selected zone</button><button class="btn s" onclick="move()">Move stock</button></div></section><section class="panel"><div class="head"><div><p class="ey">Selected compartment</p><h2>${zone}</h2></div><span class="tag ${low().length?'amber':'green'}">${low().length} low</span></div><div class="parts">${ps.map(p=>part(p,'Truck')).join('')||'<div class="empty">No parts assigned here.</div>'}</div></section></div>`}
function part(p,l){let x=stock(p,l);return `<div class="part"><div class="parttop"><div><p class="ey">${p.cat}</p><h3>${p.name}</h3><p>${p.sku}</p></div><div class="qty ${x.q<=p.min?'low':''}"><b>${x.q}</b><small>${l}</small></div></div><div class="meta"><span>Location<b>${x.z}</b></span><span>Cost<b>${M(p.cost)}</b></span><span>Sell<b>${M(p.price)}</b></span></div><div class="pacts"><div class="step"><button class="ib" onclick="adj('${p.id}','${l}',-1)">−</button><button class="ib" onclick="adj('${p.id}','${l}',1)">＋</button></div><button class="btn s" onclick="move('${p.id}')">Move</button></div></div>`}function adj(id,l,d){let p=s.parts.find(x=>x.id==id);p.s[l].q=Math.max(0,p.s[l].q+d);save();render()}
func