tion inventory(){let ps=s.parts.filter(p=>[p.name,p.sku,p.cat,stock(p,loc).z].join(' ').toLowerCase().includes(q.toLowerCase())&&(flt=='all'||flt=='low'&&stock(p,loc).q<=p.min||flt=='unsorted'&&stock(p,loc).z=='Unsorted / Floor'));v.innerHTML=`<div class="head"><div><p class="ey">All locations</p><h1>Inventory</h1><span class="muted">${M(val(loc))} at cost in ${loc}</span></div><button class="btn p" onclick="intake()">＋ Add part</button></div><div class="toolbar"><div class="search"><input value="${q}" oninput="q=this.value;inventory()" placeholder="Search part, SKU, category, or compartment"></div><div class="seg"><button class="${loc=='Truck'?'on':''}" onclick="loc='Truck';inventory()">Truck</button><button class="${loc=='Shop'?'on':''}" onclick="loc='Shop';inventory()">Shop</button></div><div class="seg"><button class="${flt=='all'?'on':''}" onclick="flt='all';inventory()">All</button><button class="${flt=='low'?'on':''}" onclick="flt='low';inventory()">Low</button><button class="${flt=='unsorted'?'on':''}" onclick="flt='unsorted';inventory()">Unsorted</button></div></div><div class="table"><table><thead><tr><th>Part</th><th>SKU</th><th>Category</th><th>Location</th><th>Qty</th><th>Min</th><th>Cost</th><th>Sell</th><th></th></tr></thead><tbody>${ps.map(p=>{let x=stock(p,loc);return `<tr><td><b>${p.name}</b></td><td>${p.sku}</td><td>${p.cat}</td><td>${x.z}</td><td><span class="tag ${x.q<=p.min?'amber':'green'}">${x.q}</span></td><td>${p.min}</td><td>${M(p.cost)}</td><td>${M(p.price)}</td><td><button class="btn s" onclick="move('${p.id}')">Move</button></td></tr>`}).join('')}</tbody></table></div>`}
function invoices(){let u=s.inv.filter(i=>i.status=='unpaid').reduce((a,i)=>a+i.total,0);v.innerHTML=`<div class="head"><div><p class="ey">Customer billing</p><h1>Invoices</h1><span class="muted">Finalized material lines deduct Truck inventory.</span></div><button class="btn p" onclick="newInv()">＋ New invoice</button></div><div class="grid metrics"><div class="metric"><i>✓</i><span>Collected</span><b>${M(s.inv.filter(i=>i.status=='paid').reduce((a,i)=>a+i.total,0))}</b></div><div class="metric"><i>!</i><span>Outstanding</span><b>${M(u)}</b></div><div class="metric"><i>▤</i><span>Invoices</span><b>${s.inv.length}</b></div><div class="metric"><i>↗</i><span>Average ticket</span><b>${M(s.inv.reduce((a,i)=>a+i.total,0)/s.inv.length)}</b></div></div><div class="table" style="margin-top:12px"><table><thead><tr><th>Invoice</th><th>Customer</th><th>Work</th><th>Date</th><th>Status</th><th class="right">Total</th><th></th></tr></thead><tbody>${s.inv.map(i=>`<tr><td><b>${i.num}</b></td><td>${i.cust}</td><td>${i.work}</td><td>${i.date}</td><td><span class="tag ${i.status=='paid'?'green':'amber'}">${i.status}</span></td><td class="right"><b>${M(i.total)}</b></td><td><button class="btn s" onclick="iStatus('${i.id}')">Mark ${i.status=='paid'?'unpaid':'paid'}</button></td></tr>`).join('')}</tbody></table></div>`}function iStatus(id){let i=s.inv.find(x=>x.id==id);i.status=i.status=='paid'?'unpaid':'paid';save();invoices()}
function bills(){let soon=s.bills.filter(b=>b.status!='paid'&&b.due>=D()&&b.due<=A(D(),7)).reduce((a,b)=>a+b.amt,0);v.innerHTML=`<div class="head"><div><p class="ey">Cash obligations</p><h1>Bills</h1><span class="muted">Recurring expenses, due dates, and payment status.</span></div><button class="btn p" onclick="newBill()">＋ Add bill</button></div><div class="grid metrics"><div class="metric"><i>!</i><span>Overdue</span><b>${